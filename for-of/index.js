//uso para itens que tem index

//exemplo de string, array, etc.

const nome = 'Kleber';

for(let valor of nome) {
    console.log(valor); //
}

const frutas = ['Maça', 'Pera', 'Uva']

for(let estrutura of frutas) {
    console.log(estrutura) //retorna tudo que tem no meu array frutas.
}